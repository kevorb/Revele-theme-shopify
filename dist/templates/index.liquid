{{ content_for_index }}
<button data-popup="sizing-chart" id="active_popup" >test Popup</button>

<div class="popup sizing-chart">
  <div class="middle">
    <h2>{{ pages.sizing-chart.title }}</h2>
    <div>
    {{ pages.sizing-chart.content }}
    </div>
  </div>
</div>
